# scripts — assessed 2026-08-02

## Findings
- [BLOCKER] scripts/sync_from_instance.py:205 — a version bump is seen as add(new jar)+remove(old jar), but both map to the SAME slug-named meta file: the add loop (l.195) rewrites `mods/<slug>.pw.toml` with the new filename, then the removal loop calls `have[old_jar].unlink()` on that same freshly-updated file → the mod is silently deleted from the pack, `packwiz refresh` succeeds, the change is committed and pushed to `main`, and every player loses the mod on next launch (CI's ≥200-mod floor won't catch one missing mod). Verified: metas are slug-named (`mods/lithostitched.pw.toml` holds `filename = "lithostitched-1.7.13-...jar"`), and `removed` (l.141) is computed from the pre-add `have` map. This is the never-run-live commit-and-push path, and a version bump is its single most likely first real input. | fix: after the add loop, recompute `have_after = pack_jars()` and only unlink `have_after.get(n)` when `n` still maps to a meta (a bumped mod's old filename no longer will); also skip removals whose corresponding add FAILED. Add the regression test below before the first live run.
- [BLOCKER] scripts/import_configs.py:55 — SKIP_RELATIVE omits FancyMenu runtime state, so `config/fancymenu/user_variables.db` and `config/fancymenu/video_element_controller_metas.json` were imported and SHIP TODAY (index.toml:1024, 1028) → per-player runtime state overwritten on every update, the exact file class the rubric mandates excluding (rubric §4 names both files; the overwrite mechanism is identical to the keybind bug). LEFT_OFF already flags them as "should probably be in .packwizignore" but rates impact low (a counter + a generated UUID) — the rubric's severity ruling stands; the fix is trivial. | fix: add both paths to SKIP_RELATIVE; add `/config/fancymenu/user_variables.db` and `/config/fancymenu/video_element_controller_metas.json` to `.packwizignore` (outside this slice); delete both from `config/`, run `packwiz refresh`; add them to verify_pack's ban (next finding).
- [SEV1] scripts/build_server_pack.py:110 — incremental builds never prune `build/server/`: a version-bumped or removed mod leaves its old jar in `build/server/mods/` (only `--clean` at l.85 removes it), and the zip (l.142) packs everything under BUILD → server gets BOTH versions of a mod = NeoForge duplicate-mod crash on the next update; it also poisons the documented workflow "compare server `mods/` against `build/server/mods/` and delete extras" (LEFT_OFF gotcha), which assumes the build dir is canonical. Stale configs and orphaned `.part` files linger the same way. | fix: after the download loop, compute `expected = {mods/<filename> for selected mods} ∪ {config rels}` and delete every file under BUILD (including `*.part`) not in it, before zipping.
- [SEV1] scripts/verify_pack.py:57 — check 2 bans per-player files only at the instance ROOT; the `config/` entry in ALLOWED_PACK_PREFIXES (enforced at l.110) waves through any per-player state under `config/` — the two shipped fancymenu files prove the gate has this hole today. | fix: add `BANNED_IN_CONFIG = ("config/fancymenu/user_variables.db", "config/fancymenu/video_element_controller_metas.json")` plus a pattern for generated `*_metas.json` under `config/fancymenu/`, and fail any indexed match, mirroring BANNED_AT_ROOT.
- [SEV2] scripts/sync_from_instance.py:199 — a failed `packwiz modrinth add` only prints `FAILED` (l.201): the script still commits (l.229) and pushes (l.237), the commit body lists the failed jar under `Added:` (l.224), and exit code is 0 → a partial sync ships to players under a commit message that lies about its content. | fix: collect failed adds; if any, print them, skip commit/push, and return 1 (folds into the BLOCKER fix — a failed add must also veto the paired removal).
- [SEV2] scripts/sync_from_instance.py:66 — the 429-retry loop (`for attempt in range(4)`) exhausts silently with no `else: raise`: a persistently rate-limited chunk is dropped, its jars are misreported as "not on Modrinth", and the operator is told to add them via `packwiz curseforge add` — the exact path that silently re-points Modrinth deps to CurseForge (ISSUE-2026-07-31-01). | fix: add `else: raise RuntimeError("Modrinth API still rate-limiting after 4 attempts")` to the retry loop.
- [SEV2] scripts/apply_sides.py:67 — labels are keyed on exact jar filenames but no warning is emitted for sides.json entries that match no meta: the first version bump of any labelled mod (sync's whole purpose) silently reverts it to `both` — safe by design but the curated 66/14 split erodes with zero signal. Verified 0 dead entries today; the defect is the missing detection, and the script's own comment (l.45-46) concedes jar filenames are unstable. | fix: track matched filenames during the loop and print every unmatched entry of `sides["client"]`/`sides["server"]` as a warning at the end (nonzero exit optional).
- [SEV2] scripts/apply_sides.py:81 — CLIENT_REQUIRED_DEPS is only consulted AFTER the client branch, so a sides.json/CLIENT_OVERRIDES "client" entry overrides the crash guard; and verify_pack.py:154-158 only rejects `side="server"` for these deps — `side="client"` passes verification, yet lithostitched is worldgen the SERVER also needs, so a client label would abort server mod loading with no gate. Latent (no conflict today) but both halves of the guard are one-sided. | fix: evaluate `meta.name in CLIENT_REQUIRED_DEPS` before the client branch and warn loudly on conflict; in verify_pack check 5, fail unless the recorded side is exactly `"both"`.
- [SEV2] scripts/build_default_options.py:107 — `keybindings.txt` is written unconditionally: pointed at a wrong/fresh instance (bad argv path), zero `key_` lines truncates the pack's 233 default binds to an empty file with no warning; CI's <50-line check (verify_pack.py:138) catches it only after commit, git only after diff review. | fix: `sys.exit` before writing when `len(keybinds) < 50`, mirroring verify_pack's threshold.
- [SEV2] scripts/verify_pack.py:72 — zero tests anywhere in the repo, and this file is CI's only gate: a regression here ships broken packs unchecked. Minimal Pareto set (3 short files, nothing more): (1) `tests/test_verify_pack.py` — real repo returns 0, plus a tmp-dir synthetic pack asserting each guard actually fails: a CurseForge-stolen mod, `options.txt` in the index, a CLIENT_REQUIRED_DEPS meta labelled `server` (requires the small testability refactor in cleanup below); (2) `tests/test_sync_removals.py` — regression for the BLOCKER: extract the removal decision into a pure helper and assert a version-bumped filename is NOT unlinked while a genuine removal is; (3) `tests/test_curseforge_url.py` — the file-id 4-digit CDN split in build_server_pack.py:53, the one load-bearing derivation with no other check. | fix: as listed; do not expand beyond these.
- [cleanup] scripts/sync_from_instance.py:48 — bandit B324 (SHA1): verified non-security use — Modrinth's `version_files` API is keyed by sha1 for content addressing | fix: `hashlib.sha1(usedforsecurity=False)`; same flag in build_server_pack.py:35 (`hashlib.new(fmt, usedforsecurity=False)`).
- [cleanup] scripts/build_server_pack.py:68 — bandit B310: URL scheme unconstrained; URLs come from repo-controlled `.pw.toml` metadata or the constructed https CDN path, and content is hash-verified (all 257 metas carry sha1/sha512 — verified), so risk is low, but a one-line guard closes it | fix: in `download()`, `if urllib.parse.urlsplit(url).scheme != "https": raise ValueError(...)`. (sync_from_instance.py:67 B310 is a false positive — the URL is a fixed `https://api.modrinth.com` constant.)
- [cleanup] scripts/build_server_pack.py:70 — unknown/missing `hash-format` silently skips BOTH the cache check (l.59) and hash verification: latent today (all metas sha1/sha512), but a future packwiz format change would disable integrity checking without a word | fix: print a per-file warning (or fail) when verification is skipped.
- [cleanup] scripts/build_server_pack.py:114 — `KeyError` absent from the except tuple: one malformed meta (missing `download`/`filename`/`update.curseforge`) aborts the whole build with a traceback instead of counting as one failed mod | fix: add `KeyError` to the tuple.
- [cleanup] scripts/sync_from_instance.py:101 — `.sync-instance-path` is persisted BEFORE the instance is validated (mods/ check is l.110): one typo'd invocation is remembered and every later bare run errors against the bad path | fix: move `cfg.write_text(str(inst))` after the `mods/` check. (Verified `.sync-instance-path` is in both `.gitignore` and `.packwizignore`, so `git add -A` cannot ship it.)
- [cleanup] scripts/verify_pack.py:64 — module-level mutable `failures`/`notes` and repo-fixed `PACK`; `indexed` (l.170) is defined only inside the else-branch at l.106 and avoids NameError only because the fail path returns early | fix: move state into `main(pack: pathlib.Path = PACK)` — this is also the refactor that unlocks the test set above.
- [cleanup] scripts/apply_sides.py:65, scripts/build_default_options.py:82 — `def main():` untyped (rest of the slice annotates) | fix: `-> None` / `-> int` respectively.

## Rubric coverage
| Category | Status |
|---|---|
| 1 Resource lifecycle | 2 findings (stale build dir SEV1; `.part`/KeyError cleanups — the `.part`+rename atomic download itself is correct) |
| 2 Concurrency & scale | ok (sequential single-operator CLIs; Modrinth lookups chunked at 100 with throttle; no unbounded accumulation) |
| 3 Security & compliance | 3 findings (B324/B310 cleanups verified as content-addressing/repo-controlled; no secrets logged; import_configs correctly excludes the Patreon key and voicechat account files) |
| 4 Domain correctness | 6 findings (per-player state ships — BLOCKER; sync deletes bumped mods — BLOCKER; verify config/ hole; sides.json drift; one-sided CLIENT_REQUIRED_DEPS guard; keybind truncation) |
| 5 LLM SDK | n/a (no LLM) |
| 6 Cleanliness & typing | 2 findings (verify_pack globals; two untyped mains) — no TODOs, no dead code, comments are load-bearing |
| 7 Error handling / API | 3 findings (partial-sync commit/push exits 0; silent 429 exhaustion; silent hash-verification skip) — not a router module; CLI exit codes assessed instead |
| 8 Config & paths | ok (all paths derive from `PACK = Path(__file__).resolve().parent.parent`; hardcoded DEFAULT_INSTANCE is a documented argv-overridable fallback; one cleanup on marker-write ordering) |

## Module verdict
has BLOCKER — the untested commit-and-push sync path silently deletes any version-bumped mod from the pack and pushes it to players, and per-player FancyMenu runtime state is in the shipped index today; both fixes are small and must land before the first live sync.
