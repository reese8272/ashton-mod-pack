# Exported pack artifacts — built by CI, never committed
*.zip
*.mrpack

# packwiz local cache
.packwizcache/

# Python bytecode — verify_pack.py imports apply_sides, so running it writes this
__pycache__/
*.pyc

# OS noise
.DS_Store
Thumbs.db
desktop.ini
.sync-instance-path
build/
